TYPEMAP
struct authensasl *	AuthenSASLtype

INPUT
AuthenSASLtype
  if (SvTYPE($arg) != SVt_RV) { printf(\"First parameter was not a reference.\\n\"); XSRETURN(0); }
  $var = (struct authensasl *)SvIV(SvRV($arg));

OUTPUT
AuthenSASLtype
  sv_setref_iv($arg, "Authen::SASL::Cyrus", (int)$var);
